angular.module(window.mss.appName).controller(
		window.mss.constants.CONTROLLER.COURSE_SELECT_CONTROLLER,
		function($scope, $http, $window) {
			console.log("courseselectController");

		}		
);
